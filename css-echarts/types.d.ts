import {AllProps as BaseAllProps, EndUserProps as BaseEndUserProps} from '../css-charts/types';

export interface EndUserProps extends BaseEndUserProps {
}

export interface AllProps extends EndUserProps, BaseAllProps {
}

